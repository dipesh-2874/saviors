<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Saviors</title>
</head>
<body class="bg-zinc-800 text-white">
    <div class="relative min-h-screen flex items-center justify-center px-4 select-none">
        <!-- BACKGROUND -->
        <div class="absolute inset-0 bg-[url(/images/mapbg.png)] bg-cover bg-center blur-[3px]"></div>
        <div class="absolute inset-0 bg-black/40"></div>
        <!-- LOGIN FORM -->
        <form action="/userlogin" method="post" class="relative z-10 w-full max-w-sm bg-zinc-800 rounded-xl shadow-xl p-6 flex flex-col gap-4">
            <h2 class="text-xl font-bold text-center">Logging in as User</h2>
            <input type="email" name="email" required placeholder="Email" class="w-full h-10 px-4 rounded-lg bg-zinc-800 border border-zinc-600">
            <input type="password" name="password" required placeholder="Password" class="w-full h-10 px-4 rounded-lg bg-zinc-800 border border-zinc-600">
            <!-- TOREGISTER -->
            <p class="text-xs text-right">Don’t have an account?
                <a href="/register" class="text-blue-400">Register</a>
            </p>
            <!-- LOGIN BUTTON -->
            <button type="submit" class="h-10 bg-blue-500 rounded-lg font-bold hover:bg-blue-600 transition">Login</button>
        </form>
    </div>
</body>
</html>